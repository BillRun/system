<?php 
// print_R($this->data); die;
?>
<div class="container component">
	<?php $this->display('w-toolbar.phtml', array()); ?>
	<h2>Wholesale</h2>
	<h3>Incoming</h3>
	<table border="1">
		<?php
		$keys = array(
			'group_by',
			'duration',
		);
		//print table headers
		echo "<tr>";
		foreach ($keys as $k) {
			echo "<th>" . ($k=='group_by'? $this->group_value : $k) . "</th>";
		}
		echo "</tr>";

		foreach ($this->data['incoming_call'] as $item) {
			echo "<tr>";
			foreach ($keys as $k) :
				echo "<td>" . (isset($item[$k]) ? $item[$k] : '&nbsp;') . "</td>";
			endforeach;
			echo "<tr>";
		} ?>
	</table>
	<h3>Outgoing</h3>
	<table border="1">
		<?php
		
		$keys = array(
			'group_by',
			'duration',
		);
		//print table headers
		echo "<tr>";
		foreach ($keys as $k) {
			echo "<th>" . ($k=='group_by'? $this->group_value : $k) . "</th>";
		}
		echo "</tr>";

		foreach ($this->data['outgoing_call'] as $item) {
			echo "<tr>";
			foreach ($keys as $k) :
				echo "<td>" . (isset($item[$k]) ? $item[$k] : '&nbsp;') . "</td>";
			endforeach;
			echo "<tr>";
		} ?>
	</table>
	
	<hr>

	<footer>
		<p>&copy; S.D.O.C. LTD 2012-2014</p>
	</footer>

</div> <!-- /container -->
