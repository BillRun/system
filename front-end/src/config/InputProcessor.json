{
  "dynamicFieldMapping": {
    "conditions": [
      { "id": "$eq", "title": "Equal" },
      { "id": "$ne", "title": "Does not equal" },
      { "id": "$in", "title": "In" },
      { "id": "$nin", "title": "Not in" },
      { "id": "$regex", "title": "Regex" }
    ]
  }
}
